import { Link } from 'react-router-dom';
import { Star } from 'lucide-react';
import { Product, formatMoney } from '../lib/api';
export function ProductCard({p}:{p:Product}){return <div className="card overflow-hidden"><div className="relative h-56 bg-primary"><img src={p.imagem_principal} alt={p.nome} className="w-full h-full object-cover"/>{p.destaque&&<span className="badge bg-accent text-primary absolute top-4 left-4">Destaque</span>}</div><div className="p-5"><p className="text-sm text-gray-500">{p.categoria_nome}</p><h3 className="font-display text-xl text-primary mt-1">{p.nome}</h3><p className="text-gray-600 text-sm mt-2 min-h-10">{p.descricao}</p><div className="flex items-center gap-1 mt-4 text-warning"><Star size={16} fill="currentColor"/><span className="text-sm font-bold text-gray-700">{Number(p.avaliacao_media||5).toFixed(1)}</span></div><div className="flex items-end justify-between mt-4"><div><b className="text-2xl text-primary">{formatMoney(p.preco)}</b>{p.preco_original&&<p className="line-through text-gray-400 text-sm">{formatMoney(p.preco_original)}</p>}</div><Link className="btn btn-primary" to={`/produto/${p.slug}`}>Ver Detalhes</Link></div></div></div>}
